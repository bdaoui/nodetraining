const chalk = require("chalk");

const http = require('http'); // loading http package
const port = 3000; // gateway to my code

console.log(chalk.blue("hello world"));


const server = http.createServer( (request, response) =>{
    
})
